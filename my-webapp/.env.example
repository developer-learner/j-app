# Firebase Configuration (populated by Firebase CLI / init.js)
# These are injected at deploy time via /__/firebase/init.js
# No manual configuration needed for local development.

# Supabase (future — Phase 2 migration)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
